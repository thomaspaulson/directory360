<?php 
if(isset($this->messages)){
	$message = implode($this->messages); 
	$this->title = $message;
	echo '<div id="message" class="notice">'.$message.'</div>';
}
else
	$this->title = "Manage user";
	
$this->headTitle($this->title);
?>
<h3>Manage page</h3>
<p><a href="<?php echo $this->url(array('module'=>'admin','controller'=>'user',
'action'=>'add'),'default',true);?>">Add new</a></p>
<table>
<tr>
	<th>First name</th>
	<th>Status</th>
	<th>Created</th>
	<th>Action</th>
</tr>
<?php
foreach($this->records as $r){
	echo "<tr>\n";
		echo "<td>".$r['FirstName']."</td>\n";
		echo "<td>";
		if($r['Status'])
			echo 'Published';
		else
			echo 'Draft';
		echo "</td>\n";		
		echo "<td>".$r['Created']."</td>\n";
		echo "<td>";
		echo '<a href="'.$this->url(array('module'=>'admin',
		'controller'=>'user','action'=>'edit','id'=>$r['ID']),'default',true).'">Edit</a>';
		echo ' | ';
		echo '<a href="'.$this->url(array('module'=>'admin',
		'controller'=>'user','action'=>'delete','id'=>$r['ID']),'default',true).'">Delete</a>';		
		echo "</td>\n";	
	echo "</tr>\n";	
}
?>
</table>